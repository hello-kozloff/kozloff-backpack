import React from 'react';
import ReactDOM from 'react-dom/client';

ReactDOM
  .createRoot((
    document.getElementById('root') as HTMLElement
  ))
  .render((
    <h1>Hello kozloff-backpack!</h1>
  ));
